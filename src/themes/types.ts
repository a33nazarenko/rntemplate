export interface ITheme {
    dark: boolean,
    colors: {
        primary: string,
        background: string,
        text: string,
        card: string,
        border: string,
        notification: string
    }
}
